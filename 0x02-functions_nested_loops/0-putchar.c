#include "main.h"
/**
 * Main - Entry Point Of Programs
 * Variables
 *
 * Return : Always 0 (Success)
 */
char _putchar(void)
{
char letter;
char ns = '_';
putchar (ns);
letter = 'p';
putchar (letter);
letter = 'u';
putchar (letter);
letter = 't';
putchar (letter);
letter = 'c';
putchar (letter);
letter = 'h';
putchar (letter);
letter = 'a';
putchar (letter);
letter = 'r';
putchar(letter);
putchar('\n');
return (0);
}
int main(void)
{
_putchar();
return (0);
}
